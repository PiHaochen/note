# GPU 结构详解

## 目录
1. [GPU 概述](#一gpu-概述)
2. [GPU 整体架构](#二gpu-整体架构)
3. [内存层次结构](#三内存层次结构)
4. [SM 流式多处理器](#四sm-流式多处理器)
5. [计算单元详解](#五计算单元详解)
6. [线程执行模型](#六线程执行模型)
7. [GPU vs CPU](#七gpu-vs-cpu)
8. [实际 GPU 规格](#八实际-gpu-规格)
9. [性能优化原理](#九性能优化原理)
10. [总结](#十总结)

---

## 一、GPU 概述

### 什么是 GPU？

**GPU（Graphics Processing Unit，图形处理单元）** 是一种专门设计用于**大规模并行计算**的处理器。

### 核心特点

- **大量核心**：几千到上万个计算核心
- **高吞吐量**：适合数据并行任务
- **专用硬件**：矩阵运算、纹理处理等专用单元
- **高带宽内存**：HBM（High Bandwidth Memory）

### 主要应用

1. **图形渲染**：游戏、3D 建模
2. **深度学习**：训练和推理
3. **科学计算**：物理模拟、气象预测
4. **加密货币挖矿**
5. **视频处理**：编解码、特效

---

## 二、GPU 整体架构

### 2.1 顶层架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        GPU 芯片                              │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │   SM 0   │  │   SM 1   │  │   SM 2   │  │  SM ...  │    │
│  │          │  │          │  │          │  │          │    │
│  │ Shared   │  │ Shared   │  │ Shared   │  │ Shared   │    │
│  │ Memory   │  │ Memory   │  │ Memory   │  │ Memory   │    │
│  │ L1 Cache │  │ L1 Cache │  │ L1 Cache │  │ L1 Cache │    │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │
│       │             │             │             │           │
│       └─────────────┴─────────────┴─────────────┘           │
│                          ↓                                   │
│       ┌──────────────────────────────────────────┐           │
│       │         L2 Cache (共享缓存)               │           │
│       │         (几 MB，所有 SM 共享)             │           │
│       └──────────────────┬───────────────────────┘           │
│                          ↓                                   │
│       ┌──────────────────────────────────────────┐           │
│       │    HBM (High Bandwidth Memory)           │           │
│       │    显存 / 全局内存 (40-80 GB)            │           │
│       └──────────────────────────────────────────┘           │
│                                                               │
└─────────────────────────────────────────────────────────────┘
                          ↕ PCIe
┌─────────────────────────────────────────────────────────────┐
│                      CPU + 主内存                             │
│                   DRAM (>1 TB)                               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 核心组件

| 组件 | 数量 | 功能 |
|------|------|------|
| **SM** | 几十到上百个 | 流式多处理器，核心计算单元 |
| **L2 Cache** | 1 个 | 所有 SM 共享的二级缓存 |
| **HBM** | 1 个 | 高带宽显存，存储数据 |
| **Memory Controller** | 多个 | 控制内存访问 |
| **PCIe Interface** | 1 个 | 与 CPU 通信 |

---

## 三、内存层次结构

### 3.1 内存金字塔

```
                    ┌─────────────┐
                    │   寄存器     │  速度: 最快 (1 cycle)
                    │  Registers   │  容量: 最小 (几 KB/线程)
                    └──────┬──────┘  作用域: 线程私有
                           │
                    ┌──────┴──────┐
                    │ Shared Memory│  速度: 极快 (~5 cycles)
                    │  + L1 Cache  │  容量: 小 (48-96 KB/SM)
                    └──────┬──────┘  作用域: Block 内共享
                           │
                    ┌──────┴──────┐
                    │  L2 Cache    │  速度: 快 (~200 cycles)
                    │              │  容量: 中 (几 MB)
                    └──────┬──────┘  作用域: 全局共享
                           │
                    ┌──────┴──────┐
                    │     HBM      │  速度: 慢 (~400-800 cycles)
                    │  (显存/VRAM) │  容量: 大 (40-80 GB)
                    └──────┬──────┘  作用域: 全局
                           │
                    ┌──────┴──────┐
                    │  CPU DRAM    │  速度: 极慢 (PCIe 传输)
                    │  (主内存)    │  容量: 巨大 (>1 TB)
                    └─────────────┘  作用域: 系统全局
```

### 3.2 内存性能对比

#### 带宽与容量（以 NVIDIA A100 为例）

```
┌─────────────────────────────────────────────────────────────┐
│              内存层次：带宽 vs 容量                          │
└─────────────────────────────────────────────────────────────┘

层级              带宽              容量           延迟
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
寄存器           ~19 TB/s          ~10 KB/线程     1 cycle
Shared Memory    ~19 TB/s          48-96 KB/SM     ~5 cycles
L1 Cache         ~19 TB/s          128 KB/SM       ~30 cycles
L2 Cache         ~7 TB/s           40 MB           ~200 cycles
HBM (显存)       1.5-2 TB/s        40-80 GB        ~400-800 cycles
CPU DRAM         12.8 GB/s         >1 TB           ~10,000+ cycles
```

#### 速度对比（相对于 HBM）

| 内存类型 | 相对速度 | 绝对延迟 |
|---------|---------|---------|
| 寄存器 | **800x** | 1 cycle |
| Shared Memory | **160x** | 5 cycles |
| L1 Cache | **27x** | 30 cycles |
| L2 Cache | **4x** | 200 cycles |
| HBM | **1x** (基准) | 800 cycles |
| CPU DRAM | **0.008x** | 10,000+ cycles |

**关键洞察**：
> Shared Memory 比 HBM 快约 **160 倍**！这就是为什么 FlashAttention 等优化算法要尽量让数据停留在 Shared Memory 中。

### 3.3 内存容量对比

```
容量对比（对数刻度）:

1 PB  ─┤
      │
100 TB─┤  ████ CPU DRAM (>1 TB)
      │
10 TB ─┤
      │
1 TB  ─┤
      │
100 GB─┤  ██ HBM (40-80 GB)
      │
10 GB ─┤
      │
1 GB  ─┤
      │
100 MB─┤  █ L2 Cache (40 MB)
      │
10 MB ─┤  █ Shared Memory (20 MB 总计)
      │
1 MB  ─┤
      │
100 KB─┤  █ L1 Cache (128 KB/SM)
      │
10 KB ─┤  █ Registers (10 KB/线程)
      │
1 KB  ─┤
```

---

## 四、SM（流式多处理器）

### 4.1 SM 是什么？

**SM (Streaming Multiprocessor)** 是 GPU 的**核心计算单元**，类似于 CPU 的核心，但功能更简单、数量更多。

- 一个现代 GPU 有 **几十到上百个 SM**
- 每个 SM 可以同时执行 **几千个线程**
- SM 是**独立的计算单元**，有自己的寄存器和共享内存

### 4.2 SM 内部结构

```
┌─────────────────────────────────────────────────────────────┐
│              SM (Streaming Multiprocessor)                   │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │         CUDA Cores (FP32 单元)                     │     │
│  │  ████████████████████████████████ (64-128 个)      │     │
│  │  每个可执行: 加法、乘法、FMA 等                    │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │         Tensor Cores (矩阵运算单元)                │     │
│  │  ████████ (4-8 个)                                  │     │
│  │  专门用于矩阵乘法 (如 4×4 矩阵)                    │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │         FP64 Cores (双精度单元)                    │     │
│  │  ████ (较少，通常是 FP32 的 1/2 或 1/32)           │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │    Shared Memory / L1 Cache (可配置)               │     │
│  │    ┌──────────────┬──────────────┐                 │     │
│  │    │ Shared Memory│  L1 Cache    │                 │     │
│  │    │   64 KB      │   64 KB      │ (可调整比例)    │     │
│  │    └──────────────┴──────────────┘                 │     │
│  │    总容量: 48-128 KB (取决于架构)                  │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │         Register File (寄存器堆)                   │     │
│  │         容量: 256 KB (所有线程共享)                │     │
│  │         每个线程: 最多 255 个 32-bit 寄存器        │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │         Warp Scheduler (线程束调度器)              │     │
│  │         - 管理 warp (32 个线程) 的执行             │     │
│  │         - 指令分发                                 │     │
│  │         - 处理分支分歧                             │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │         Load/Store Units (内存访问单元)            │     │
│  │         负责从全局内存加载/存储数据                │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │         Special Function Units (SFU)               │     │
│  │         执行特殊函数: sin, cos, sqrt, exp 等       │     │
│  └────────────────────────────────────────────────────┘     │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 SM 的关键参数（NVIDIA A100）

| 参数 | 值 | 说明 |
|------|-----|------|
| **SM 总数** | 108 | 整个 GPU |
| **CUDA Cores/SM** | 64 | 每个 SM |
| **Tensor Cores/SM** | 4 | 第 3 代 |
| **FP64 Cores/SM** | 32 | 双精度 |
| **寄存器/SM** | 256 KB | 所有线程共享 |
| **Shared Memory/SM** | 最大 164 KB | 可配置 |
| **L1 Cache/SM** | 128 KB | 与 Shared Memory 共享 |
| **最大线程/SM** | 2048 | 同时驻留 |
| **最大 Warp/SM** | 64 | 2048/32 |
| **最大 Block/SM** | 32 | 取决于资源 |

---

## 五、计算单元详解

### 5.1 CUDA Core（CUDA 核心）

**CUDA Core** 是 GPU 的基本计算单元，类似于 CPU 的 ALU。

#### 功能
- 执行**单精度浮点运算**（FP32）
- 基本操作：加法、乘法、FMA（融合乘加）
- 整数运算
- 逻辑运算

#### 特点
```
单个 CUDA Core 的能力:
  - 1 个 FP32 FMA/cycle (a × b + c)
  - 简单的控制逻辑
  - 比 CPU 核心弱很多
  
但是数量多:
  - A100: 6912 个 CUDA Cores
  - H100: 14592 个 CUDA Cores
  
总算力 = 单核性能 × 核心数量
```

#### 计算示例
```cuda
// 向量加法（每个 CUDA Core 处理一个元素）
__global__ void vectorAdd(float *a, float *b, float *c, int n) {
    int i = blockIdx.x * blockDim.x + threadIdx.x;
    if (i < n) {
        c[i] = a[i] + b[i];  // 在 CUDA Core 上执行
    }
}
```

---

### 5.2 Tensor Core（张量核心）

**Tensor Core** 是专门用于**矩阵运算**的硬件加速器，是深度学习性能的关键。

#### 功能
- 执行**矩阵乘法**：$D = A \times B + C$
- 一次操作处理整个矩阵块（如 4×4, 8×8, 16×16）
- 支持多种精度：FP16, BF16, TF32, INT8, FP8

#### 性能对比

```
计算 C = A × B (矩阵乘法)

使用 CUDA Cores:
  ┌─────────────────────────────────┐
  │ for i in range(M):              │
  │   for j in range(N):            │
  │     for k in range(K):          │
  │       C[i][j] += A[i][k]*B[k][j]│
  │                                 │
  │ 需要 M×N×K 次 FMA 操作          │
  │ 逐个元素计算                    │
  └─────────────────────────────────┘
  性能: 1x (基准)

使用 Tensor Cores:
  ┌─────────────────────────────────┐
  │ 一次操作计算整个 16×16 矩阵块  │
  │                                 │
  │ D[16×16] = A[16×16] × B[16×16]  │
  │          + C[16×16]             │
  │                                 │
  │ 1 个操作 = 16×16×16 = 4096 FMA  │
  └─────────────────────────────────┘
  性能: 8-16x 更快
```

#### Tensor Core 代数

**第 3 代 Tensor Core（A100）**：
- 矩阵大小：16×8×16（M×N×K）
- 精度：FP16, BF16, TF32, INT8
- 吞吐量：312 TFLOPS (FP16)

**第 4 代 Tensor Core（H100）**：
- 矩阵大小：16×8×16
- 新增 FP8 支持
- 吞吐量：1000+ TFLOPS (FP8)

#### 使用示例
```cuda
// 使用 Tensor Core 的矩阵乘法（通过 WMMA API）
#include <mma.h>
using namespace nvcuda::wmma;

__global__ void matmul_tensor_core() {
    // 声明矩阵片段
    fragment<matrix_a, 16, 16, 16, half, row_major> a_frag;
    fragment<matrix_b, 16, 16, 16, half, col_major> b_frag;
    fragment<accumulator, 16, 16, 16, half> c_frag;
    
    // 加载数据
    load_matrix_sync(a_frag, a, 16);
    load_matrix_sync(b_frag, b, 16);
    fill_fragment(c_frag, 0.0f);
    
    // 矩阵乘法（在 Tensor Core 上执行）
    mma_sync(c_frag, a_frag, b_frag, c_frag);
    
    // 存储结果
    store_matrix_sync(c, c_frag, 16, mem_row_major);
}
```

---

### 5.3 Special Function Units (SFU)

**SFU** 用于执行特殊数学函数。

#### 功能
- 超越函数：`sin()`, `cos()`, `tan()`
- 指数函数：`exp()`, `log()`
- 平方根：`sqrt()`, `rsqrt()`
- 倒数：`rcp()`

#### 特点
- 速度快但精度略低
- 每个 SM 有少量 SFU（通常 4-8 个）
- 多个线程共享，可能成为瓶颈

---

## 六、线程执行模型

### 6.1 线程层次结构

```
Grid (网格)
┌─────────────────────────────────────────────────────────┐
│                                                           │
│  Block (0,0)       Block (1,0)       Block (2,0)         │
│  ┌───────────┐    ┌───────────┐    ┌───────────┐        │
│  │ Warp 0    │    │ Warp 0    │    │ Warp 0    │        │
│  │ ████████  │    │ ████████  │    │ ████████  │        │
│  │ Warp 1    │    │ Warp 1    │    │ Warp 1    │        │
│  │ ████████  │    │ ████████  │    │ ████████  │        │
│  │ ...       │    │ ...       │    │ ...       │        │
│  └───────────┘    └───────────┘    └───────────┘        │
│                                                           │
│  Block (0,1)       Block (1,1)       Block (2,1)         │
│  ┌───────────┐    ┌───────────┐    ┌───────────┐        │
│  │ Warp 0    │    │ Warp 0    │    │ Warp 0    │        │
│  │ ████████  │    │ ████████  │    │ ████████  │        │
│  │ ...       │    │ ...       │    │ ...       │        │
│  └───────────┘    └───────────┘    └───────────┘        │
│                                                           │
└─────────────────────────────────────────────────────────┘

█ = Thread (线程，最小执行单元)
████████ = Warp (32 个连续线程)
```

### 6.2 层次说明

#### 1. Thread（线程）
- **最小执行单元**
- 每个线程有唯一 ID
- 执行相同的 kernel 代码
- 有私有的寄存器

#### 2. Warp（线程束）
- **32 个连续线程**组成
- **硬件调度的基本单位**
- 同一 warp 内的线程**同步执行**（SIMT）
- 共享指令指针

**关键特性：SIMT（Single Instruction, Multiple Threads）**
```
Warp 执行示例:

时钟周期 1: 所有 32 个线程执行指令 1
时钟周期 2: 所有 32 个线程执行指令 2
时钟周期 3: 所有 32 个线程执行指令 3
...

如果有分支:
  if (threadIdx.x < 16) {
      // 指令 A
  } else {
      // 指令 B
  }

执行过程:
  时钟周期 1: 前 16 个线程执行 A，后 16 个线程空闲
  时钟周期 2: 前 16 个线程空闲，后 16 个线程执行 B
  
结果: 性能下降 50%（分支分歧）
```

#### 3. Block（线程块）
- 由多个线程组成（通常 128-1024 个）
- 可以是 1D, 2D, 或 3D 结构
- 同一 block 内的线程：
  - 可以通过 **Shared Memory** 通信
  - 可以通过 `__syncthreads()` 同步
- 被分配到一个 SM 上执行

#### 4. Grid（网格）
- 由多个 block 组成
- 可以是 1D, 2D, 或 3D 结构
- 不同 block 之间**不能直接通信**
- Block 可以在不同 SM 上并行执行

### 6.3 线程索引计算

```cuda
// 1D 索引
int tid = blockIdx.x * blockDim.x + threadIdx.x;

// 2D 索引
int x = blockIdx.x * blockDim.x + threadIdx.x;
int y = blockIdx.y * blockDim.y + threadIdx.y;
int tid = y * width + x;

// 3D 索引
int x = blockIdx.x * blockDim.x + threadIdx.x;
int y = blockIdx.y * blockDim.y + threadIdx.y;
int z = blockIdx.z * blockDim.z + threadIdx.z;
int tid = z * width * height + y * width + x;
```

### 6.4 执行流程

```
Kernel 启动
    ↓
Grid 被分解为多个 Block
    ↓
每个 Block 被分配到一个 SM
    ↓
Block 被分解为多个 Warp (每个 32 线程)
    ↓
Warp Scheduler 调度 Warp 执行
    ↓
Warp 中的线程在 CUDA Cores 上执行
    ↓
所有线程完成
    ↓
Kernel 结束
```

---

## 七、GPU vs CPU

### 7.1 架构对比

```
CPU 架构（少核心，强单核）:
┌────────────────────────────────────────────────────────┐
│  Core 1         Core 2         Core 3         Core 4   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐ │
│  │  ALU     │  │  ALU     │  │  ALU     │  │  ALU   │ │
│  │  ALU     │  │  ALU     │  │  ALU     │  │  ALU   │ │
│  │          │  │          │  │          │  │        │ │
│  │ 复杂控制 │  │ 复杂控制 │  │ 复杂控制 │  │复杂控制│ │
│  │ 分支预测 │  │ 分支预测 │  │ 分支预测 │  │分支预测│ │
│  │ 乱序执行 │  │ 乱序执行 │  │ 乱序执行 │  │乱序执行│ │
│  │          │  │          │  │          │  │        │ │
│  │ L1: 64KB │  │ L1: 64KB │  │ L1: 64KB │  │L1: 64KB│ │
│  │ L2: 512KB│  │ L2: 512KB│  │ L2: 512KB│  │L2:512KB│ │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘ │
│                                                          │
│                  L3 Cache: 32 MB (共享)                  │
└────────────────────────────────────────────────────────┘

特点:
✓ 少量核心 (4-64 个)
✓ 强大的单核性能
✓ 复杂的控制逻辑
✓ 大缓存 (MB 级)
✓ 低延迟
✓ 适合串行任务


GPU 架构（多核心，弱单核）:
┌────────────────────────────────────────────────────────┐
│  SM 1       SM 2       SM 3       ...       SM 108     │
│  ┌──────┐  ┌──────┐  ┌──────┐            ┌──────┐    │
│  │██████│  │██████│  │██████│            │██████│    │
│  │██████│  │██████│  │██████│    ...     │██████│    │
│  │██████│  │██████│  │██████│            │██████│    │
│  │ 64个 │  │ 64个 │  │ 64个 │            │ 64个 │    │
│  │ CUDA │  │ CUDA │  │ CUDA │            │ CUDA │    │
│  │ Cores│  │ Cores│  │ Cores│            │ Cores│    │
│  │      │  │      │  │      │            │      │    │
│  │简单  │  │简单  │  │简单  │            │简单  │    │
│  │控制  │  │控制  │  │控制  │            │控制  │    │
│  │      │  │      │  │      │            │      │    │
│  │Shared│  │Shared│  │Shared│            │Shared│    │
│  │96 KB │  │96 KB │  │96 KB │            │96 KB │    │
│  └──────┘  └──────┘  └──────┘            └──────┘    │
│                                                          │
│              L2 Cache: 40 MB (共享)                      │
│              HBM: 40-80 GB                               │
└────────────────────────────────────────────────────────┘

特点:
✓ 大量核心 (几千到上万个)
✓ 较弱的单核性能
✓ 简单的控制逻辑
✓ 小缓存 (KB 级/SM)
✓ 高延迟但高吞吐量
✓ 适合并行任务
```

### 7.2 性能对比

| 指标 | CPU (Intel Xeon) | GPU (NVIDIA A100) | GPU 优势 |
|------|------------------|-------------------|---------|
| **核心数** | 64 | 6912 CUDA Cores | 108x |
| **线程数** | 128 | 数百万 | 数千倍 |
| **时钟频率** | 3.0 GHz | 1.4 GHz | CPU 更高 |
| **单核性能** | 强 | 弱 | CPU 更强 |
| **FP32 性能** | ~5 TFLOPS | 19.5 TFLOPS | 4x |
| **FP16 性能** | ~10 TFLOPS | 312 TFLOPS | 31x |
| **内存带宽** | 200 GB/s | 1555 GB/s | 8x |
| **内存容量** | 512 GB - 2 TB | 40-80 GB | CPU 更大 |
| **功耗** | 270W | 400W | 相近 |
| **价格** | $5,000 | $10,000 | CPU 更便宜 |

### 7.3 适用场景

#### CPU 擅长
- ✅ 串行任务
- ✅ 复杂的控制流
- ✅ 低延迟要求
- ✅ 小数据量
- ✅ 不规则内存访问
- ✅ 通用计算

**例子**：操作系统、数据库、网页服务器

#### GPU 擅长
- ✅ 大规模并行任务
- ✅ 简单的控制流
- ✅ 高吞吐量要求
- ✅ 大数据量
- ✅ 规则内存访问
- ✅ 数据并行计算

**例子**：深度学习、图形渲染、科学计算

---

## 八、实际 GPU 规格

### 8.1 NVIDIA A100（数据中心 GPU）

```
┌─────────────────────────────────────────────────────────┐
│                    NVIDIA A100 规格                      │
├─────────────────────────────────────────────────────────┤
│ 架构:          Ampere (第 3 代)                          │
│ 制程:          7nm TSMC                                  │
│ 晶体管数:      54.2 billion                              │
│ 芯片面积:      826 mm²                                   │
├─────────────────────────────────────────────────────────┤
│ SM 数量:       108                                       │
│ CUDA Cores:    6912 (64/SM)                              │
│ Tensor Cores:  432 (4/SM, 第 3 代)                       │
│ FP64 Cores:    3456 (32/SM)                              │
├─────────────────────────────────────────────────────────┤
│ 基础频率:      1.095 GHz                                 │
│ Boost 频率:    1.410 GHz                                 │
├─────────────────────────────────────────────────────────┤
│ 内存类型:      HBM2e                                     │
│ 内存容量:      40 GB / 80 GB                             │
│ 内存带宽:      1555 GB/s (40GB) / 2039 GB/s (80GB)      │
│ 内存总线:      5120-bit                                  │
├─────────────────────────────────────────────────────────┤
│ L2 Cache:      40 MB                                     │
│ Shared Memory: 164 KB/SM (最大)                          │
│ L1 Cache:      128 KB/SM                                 │
│ 寄存器:        256 KB/SM                                 │
├─────────────────────────────────────────────────────────┤
│ FP32 性能:     19.5 TFLOPS                               │
│ FP16 性能:     312 TFLOPS (Tensor Core)                  │
│ FP64 性能:     9.7 TFLOPS                                │
│ INT8 性能:     624 TOPS (Tensor Core)                    │
├─────────────────────────────────────────────────────────┤
│ TDP:           400W (SXM) / 250W (PCIe)                  │
│ 接口:          PCIe 4.0 x16 / SXM4                       │
│ NVLink:        600 GB/s (第 3 代)                        │
├─────────────────────────────────────────────────────────┤
│ 价格:          ~$10,000 - $15,000                        │
│ 发布日期:      2020 年 5 月                              │
└─────────────────────────────────────────────────────────┘
```

### 8.2 NVIDIA H100（最新一代）

```
┌─────────────────────────────────────────────────────────┐
│                    NVIDIA H100 规格                      │
├─────────────────────────────────────────────────────────┤
│ 架构:          Hopper (第 4 代)                          │
│ 制程:          4nm TSMC                                  │
│ 晶体管数:      80 billion                                │
├─────────────────────────────────────────────────────────┤
│ SM 数量:       132                                       │
│ CUDA Cores:    14592 (128/SM)                            │
│ Tensor Cores:  528 (4/SM, 第 4 代)                       │
├─────────────────────────────────────────────────────────┤
│ 内存容量:      80 GB HBM3                                │
│ 内存带宽:      3000 GB/s                                 │
├─────────────────────────────────────────────────────────┤
│ FP32 性能:     60 TFLOPS                                 │
│ FP16 性能:     1000 TFLOPS (Tensor Core)                 │
│ FP8 性能:      2000 TFLOPS (新增)                        │
│ INT8 性能:     2000 TOPS                                 │
├─────────────────────────────────────────────────────────┤
│ TDP:           700W                                      │
│ NVLink:        900 GB/s (第 4 代)                        │
├─────────────────────────────────────────────────────────┤
│ 新特性:        Transformer Engine                        │
│               FP8 支持                                   │
│               更大的 Shared Memory                       │
│ 价格:          ~$30,000 - $40,000                        │
│ 发布日期:      2022 年 9 月                              │
└─────────────────────────────────────────────────────────┘
```

### 8.3 消费级 GPU 对比

| GPU | CUDA Cores | Tensor Cores | 内存 | 带宽 | TDP | 价格 |
|-----|-----------|--------------|------|------|-----|------|
| **RTX 4090** | 16384 | 512 (4代) | 24 GB GDDR6X | 1008 GB/s | 450W | $1,599 |
| **RTX 4080** | 9728 | 304 | 16 GB GDDR6X | 736 GB/s | 320W | $1,199 |
| **RTX 3090 Ti** | 10752 | 336 (3代) | 24 GB GDDR6X | 1008 GB/s | 450W | $1,999 |
| **RTX 3080** | 8704 | 272 | 10 GB GDDR6X | 760 GB/s | 320W | $699 |

---

## 九、性能优化原理

### 9.1 为什么需要优化？

```
理想情况 vs 实际情况:

理想情况（计算密集）:
┌────────────────────────────────────────┐
│ GPU 时间分配                            │
├────────────────────────────────────────┤
│ ████████████████████████████ 计算 90%  │
│ ██ 内存访问 10%                        │
└────────────────────────────────────────┘
GPU 利用率: 90%


实际情况（内存密集，未优化）:
┌────────────────────────────────────────┐
│ GPU 时间分配                            │
├────────────────────────────────────────┤
│ ████ 计算 10%                          │
│ ████████████████████████████ 等待内存 90%│
└────────────────────────────────────────┘
GPU 利用率: 10%

问题: 大部分时间在等待数据从 HBM 传输！
```

### 9.2 优化策略

#### 1. **最大化 SRAM 利用率**

```
原则: 让数据尽量停留在快速内存中

差的做法:
  1. 从 HBM 读取数据
  2. 计算
  3. 写回 HBM
  4. 再从 HBM 读取
  5. 再计算
  ...
  HBM 访问次数: O(N)

好的做法:
  1. 从 HBM 读取数据到 Shared Memory
  2. 在 Shared Memory 中完成所有计算
  3. 最后写回 HBM
  HBM 访问次数: O(1)
```

#### 2. **合并内存访问（Coalesced Access）**

```
未合并访问（慢）:
Warp 中的 32 个线程访问不连续的内存地址
  Thread 0: 读取 addr[0]    ─┐
  Thread 1: 读取 addr[100]  ─┤
  Thread 2: 读取 addr[200]  ─┤ 32 次独立的内存事务
  ...                        ─┤
  Thread 31: 读取 addr[3100]─┘
  
结果: 32 次内存事务，慢！


合并访问（快）:
Warp 中的 32 个线程访问连续的内存地址
  Thread 0: 读取 addr[0]    ─┐
  Thread 1: 读取 addr[1]    ─┤
  Thread 2: 读取 addr[2]    ─┤ 1 次合并的内存事务
  ...                        ─┤ (128 字节对齐)
  Thread 31: 读取 addr[31]  ─┘
  
结果: 1 次内存事务，快 32 倍！
```

#### 3. **避免 Bank Conflict**

```
Shared Memory 结构:
  - 分为 32 个 bank
  - 每个 bank 宽度 4 字节
  - 同一 warp 内的线程同时访问不同 bank: 无冲突
  - 同一 warp 内的线程同时访问同一 bank: 冲突

无冲突（好）:
  __shared__ float data[32];
  float value = data[threadIdx.x];  // 每个线程访问不同 bank
  
有冲突（差）:
  __shared__ float data[32][32];
  float value = data[0][threadIdx.x];  // 所有线程访问同一列
  // 如果列的步长是 32，会导致 bank conflict
  
解决方案: 填充（padding）
  __shared__ float data[32][33];  // 多一列，避免冲突
```

#### 4. **避免 Warp 分歧**

```
有分歧（慢）:
  if (threadIdx.x < 16) {
      // 分支 A: 10 条指令
  } else {
      // 分支 B: 10 条指令
  }
  // 执行时间 = 10 + 10 = 20 条指令

无分歧（快）:
  int value = (threadIdx.x < 16) ? valueA : valueB;
  result = compute(value);
  // 执行时间 = 1 条指令（条件选择）+ 计算时间
```

#### 5. **使用 Tensor Cores**

```
使用 CUDA Cores 的矩阵乘法:
  for (int i = 0; i < M; i++) {
      for (int j = 0; j < N; j++) {
          for (int k = 0; k < K; k++) {
              C[i][j] += A[i][k] * B[k][j];
          }
      }
  }
  性能: 19.5 TFLOPS (FP32)

使用 Tensor Cores:
  // 通过 WMMA API 或 cuBLAS
  mma_sync(C, A, B, C);  // 一次计算 16×16 矩阵块
  性能: 312 TFLOPS (FP16)
  
加速比: 16x
```

### 9.3 FlashAttention 的优化

FlashAttention 是 GPU 优化的典范，综合运用了多种技术：

```
原始 Attention 的问题:
┌─────────────────────────────────────────┐
│ 1. 计算 S = QK^T                        │
│    - 从 HBM 读取 Q, K                   │
│    - 计算                               │
│    - S 写回 HBM (N² 太大)               │
│                                         │
│ 2. 计算 P = Softmax(S)                  │
│    - 从 HBM 读取 S                      │
│    - 计算                               │
│    - P 写回 HBM                         │
│                                         │
│ 3. 计算 O = PV                          │
│    - 从 HBM 读取 P, V                   │
│    - 计算                               │
│    - O 写回 HBM                         │
│                                         │
│ HBM 访问: O(N²)                         │
└─────────────────────────────────────────┘


FlashAttention 的优化:
┌─────────────────────────────────────────┐
│ 1. 分块（Tiling）                       │
│    - 将 Q, K, V 分成小块                │
│    - 每块大小适合 Shared Memory         │
│                                         │
│ 2. 融合操作（Kernel Fusion）           │
│    - QK^T → Softmax → PV 在一个 kernel │
│    - 中间结果不写回 HBM                 │
│                                         │
│ 3. 在线 Softmax                         │
│    - 增量计算，不需要完整的 S 矩阵     │
│                                         │
│ 4. 重计算                               │
│    - 反向传播时重新计算，不存储         │
│                                         │
│ HBM 访问: O(N)                          │
│ 加速: 2-4x                              │
└─────────────────────────────────────────┘
```

---

## 十、总结

### 10.1 GPU 架构核心要点

1. **大规模并行**
   - 几千到上万个核心
   - 适合数据并行任务
   - 高吞吐量

2. **内存层次**
   - SRAM（快但小）vs HBM（慢但大）
   - 优化关键：最大化 SRAM 利用率
   - 内存带宽是瓶颈

3. **SM 是核心**
   - 独立的计算单元
   - 包含 CUDA Cores, Tensor Cores, Shared Memory
   - Warp 是调度单位

4. **专用硬件**
   - Tensor Cores：矩阵运算加速
   - SFU：特殊函数
   - Load/Store Units：内存访问

### 10.2 优化原则

```
GPU 优化的黄金法则:

1. 计算不是瓶颈，内存访问才是瓶颈
   → 减少 HBM 访问，最大化 SRAM 利用

2. 并行度是关键
   → 启动足够多的线程，隐藏延迟

3. 合并内存访问
   → 让 warp 内的线程访问连续内存

4. 避免分支分歧
   → 保持 warp 内线程执行一致

5. 使用专用硬件
   → Tensor Cores 比 CUDA Cores 快 8-16 倍
```

### 10.3 性能对比总结

| 维度 | CPU | GPU | 优势 |
|------|-----|-----|------|
| **核心数** | 少 (4-64) | 多 (几千) | GPU |
| **单核性能** | 强 | 弱 | CPU |
| **并行能力** | 低 | 高 | GPU |
| **内存带宽** | 低 (~200 GB/s) | 高 (~2 TB/s) | GPU |
| **延迟** | 低 | 高 | CPU |
| **吞吐量** | 低 | 高 | GPU |
| **功耗效率** | 低 | 高 | GPU |
| **编程难度** | 低 | 高 | CPU |

### 10.4 学习路径

```
1. 基础概念
   ├─ GPU 架构
   ├─ 内存层次
   └─ 线程模型

2. CUDA 编程
   ├─ Kernel 编写
   ├─ 内存管理
   └─ 同步机制

3. 性能优化
   ├─ 内存优化
   ├─ 计算优化
   └─ Profiling

4. 高级主题
   ├─ Tensor Cores
   ├─ 多 GPU 编程
   └─ 算法设计（如 FlashAttention）
```

---

## 参考资料

### 官方文档
- [CUDA C++ Programming Guide](https://docs.nvidia.com/cuda/cuda-c-programming-guide/)
- [CUDA C++ Best Practices Guide](https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/)
- [NVIDIA GPU Architecture Whitepapers](https://www.nvidia.com/en-us/data-center/resources/gpu-architecture/)

### 书籍
- *Programming Massively Parallel Processors* - David Kirk & Wen-mei Hwu
- *CUDA by Example* - Jason Sanders & Edward Kandrot
- *Professional CUDA C Programming* - John Cheng

### 在线资源
- [NVIDIA Developer Blog](https://developer.nvidia.com/blog)
- [CUDA Samples](https://github.com/NVIDIA/cuda-samples)
- [CUTLASS](https://github.com/NVIDIA/cutlass)

### 相关论文
- FlashAttention: Fast and Memory-Efficient Exact Attention
- Megatron-LM: Training Multi-Billion Parameter Language Models
- ZeRO: Memory Optimizations Toward Training Trillion Parameter Models

---

**文档创建时间**: 2025-11-16  
**作者**: AI Assistant  
**版本**: 1.0

